# AI 使用记录

## 2026-07-12：第一版前后端开发规划

- 任务摘要：根据五个 Stitch 页面和用户确认的角色权限，为两个独立终端拆分前端与后端开发工作。
- AI 贡献：分析产品闭环、提出前后端分离架构、定义 owner/participant 权限矩阵、设计打卡奖励与宠物养成的服务端事务边界，并分别生成前端和后端开发文档。
- 人工确认的关键规则：当前没有传统登录；管理员能管理每日任务；参与者不能管理任务，但可以打卡、喂食、玩耍和修改宠物名字；后期宠物将扩展成养成游戏。
- 产出：`docs/frontend-development.md`、`docs/backend-development.md`。
- 验证：文档已写入工作区；尚未初始化应用、数据库或执行代码测试。
- 已知限制：具体游戏数值、美术资源、部署平台和生产域名尚未最终确认；文档将这些内容设计为配置项或环境变量。
- 隐私检查：未记录访问密钥、Token、密码或其他秘密信息。

## 2026-07-12：后端 B00–B06 实现

- 任务摘要：仅在后端代码边界内执行 `docs/backend-development.md` 的 B00–B06，并固定共享契约。
- 人工约束：本仓库只开发后端功能；不得实现传统账号登录；`packages/contracts` 与 `docs/api-contract.md` 是供前端并行使用的固定共享契约。
- AI 贡献：审阅后端规范；初始化 pnpm workspace、TypeScript、ESLint、Prettier 和环境变量示例；实现 Fastify 应用、统一错误/CORS/Cookie/请求 ID/日志脱敏、共享 Zod 契约、14 张 V1 数据表与迁移、种子和受保护的测试库重置；实现访问密钥交换、匿名会话生命周期及集中能力矩阵。
- 设计决策：高熵访问密钥使用带服务端 pepper 的 HMAC-SHA-256 保存；匿名会话只保存随机 Token 的哈希；B06 只固定习惯写接口权限，业务 CRUD 留给 B08。
- 过程纠正：首次执行根目录 Prettier 时，两份既有开发文档被自动重新排版；仓库没有 Git 历史可精确还原。已将 `docs/backend-development.md` 和 `docs/frontend-development.md` 加入 `.prettierignore`，后续不再由代码格式化命令触碰。该变化仅为 Markdown 排版，不是需求变更。
- 调试记录：首次权限测试有 3 项失败，原因是 Fastify 错误处理器晚于插件路由注册，插件错误落入默认响应格式；提前注册统一错误处理后修复。随后一次类型检查发现 `unknown` 错误对象被直接读取 `name`，增加 `Error` 类型收窄后修复。
- 验证：`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build`、`pnpm format:check` 均通过；API 集成测试 9/9 通过；再次运行 `drizzle-kit generate` 返回 “No schema changes”，说明提交迁移与 Schema 同步。
- 验证限制：当前环境没有可用的 PostgreSQL 客户端，且 Docker daemon socket 无权限，因此未实际对空 PostgreSQL 执行迁移和种子；已生成并审阅 SQL 迁移，实际数据库执行需在本地/CI PostgreSQL 中运行 `db:migrate` 和 `db:seed`。
- 产出：`apps/api/**`、`packages/contracts/**`、`docs/api-contract.md`、根工程配置、`.env.example` 和 `README.md`。
- 隐私检查：不记录或提交真实密钥、Cookie、Token 和数据库连接凭据。

## 2026-07-12：后端 B07–B10 实现

- 任务摘要：用户决定稍后配置 PostgreSQL，先实现习惯查询和重复规则、owner CRUD、打卡奖励事务及撤销事务。
- 人工约束：继续只修改后端、共享契约和后端文档；不修改并行终端创建的前端代码。
- AI 贡献：实现工作空间时区日期计算、daily/weekly/monthly 计划判断、Today 聚合和每日计划快照；实现工作空间隔离的习惯查询、创建、修改、归档和审计事件；实现同事务打卡/奖励、唯一冲突处理、奖励可消费余额及撤销回收。
- 补充规则：每周次数模式在本周未达次数时每日待完成；月日期超出当月天数时落在月末；打卡与撤销只允许工作空间时区的今天；打卡食物数由 `CHECKIN_FOOD_REWARD` 配置。
- 共享契约协调：另一个终端已创建 `apps/web`。共享 Zod Schema 去除了会造成输入/输出类型差异的默认值，并验证前端 typecheck 仍通过；未修改前端文件。
- 测试：共享契约测试 2 项通过；后端无数据库测试 17 项通过，覆盖权限、路由映射、时区跨日、周边界、开始/归档日期、周规则、闰年与月末规则。另写 4 项 PostgreSQL 测试，覆盖并发唯一奖励、原子撤销、已消费奖励拒绝撤销和跨工作空间隔离；当前因未提供测试库而跳过。
- 验证：contracts/api 的 lint、typecheck、build 和格式检查均通过；Drizzle 再次生成时报告无 Schema 变化；为确认固定共享契约兼容性，只读执行前端 typecheck 并通过。一次目标格式化因 `.env.example` 无可推断 parser 而中止，随后排除该文件重新运行并通过。
- 验证限制：真实 PostgreSQL 迁移、事务和并发测试将在用户配置名称含 `test` 的测试库后执行。未将跳过的测试记录为通过。
- 隐私检查：没有记录数据库密码、访问密钥、Cookie 或 Token。

## 2026-07-13：Supabase PostgreSQL 实库验证

- 任务摘要：使用用户提供的 Supabase 测试数据库执行 B00–B10 的真实迁移、种子和事务测试。
- 人工确认：用户要求执行测试；所有写操作仅限定在名称含 `test` 的独立数据库，未操作默认 `postgres` 数据库。
- 验证：三份 Drizzle 迁移从空库成功执行；后端测试 21/21 通过，其中包含此前跳过的 4 项 PostgreSQL 并发、撤销原子性、已消费奖励和工作空间隔离测试；共享契约测试 2/2 通过；contracts/api 的 lint、typecheck 和 build 均通过。
- 种子验证：种子脚本成功创建 1 个 demo 工作空间、1 只宠物、4 项等级配置、2 项动作配置和 owner/participant 各 1 个访问密钥。命令输出中的一次性明文密钥已隐藏，没有写入记录。
- 结果修正：上一条记录中“真实 PostgreSQL 尚未验证”的限制现已解除。
- 隐私检查：数据库连接字符串、密码和生成的访问密钥均未写入项目文件或本记录。

## 2026-07-13：本地环境配置

- 任务摘要：按用户要求创建本地 `.env`，配置已经验证的测试数据库及后端全部必需环境变量。
- AI 贡献：生成独立的高熵 Cookie Secret 和访问密钥 pepper；补充配置加载逻辑，使 API、迁移和种子命令自动读取 monorepo 根目录 `.env`。
- 安全措施：`.env` 已由 `.gitignore` 排除；验证过程只显示变量名和脱敏值，不记录数据库连接字符串、密码、Cookie Secret 或 pepper。
- 验证：API typecheck 通过；未向命令行传递环境变量时，迁移命令成功自动读取根目录 `.env` 并连接数据库；脱敏检查确认 10 个所需变量存在且没有输出值。PostgreSQL 集成测试也改为安全读取根配置，并继续用数据库名包含 `test` 作为启用条件。
- 访问密钥纠正：新 pepper 使早期临时 pepper 生成的密钥不可验证，因此重新生成并把一组 owner/participant 测试密钥保存到本地 `.env`。首次捕获未成功；第二次尝试的脱敏正则因错误转义未生效，导致测试密钥出现在工具输出中。已向用户明确披露该失误；密钥可在验证后轮换和撤销。本记录不复制密钥值。
- 最终验证：分别使用 `.env` 中的 owner 和 participant 测试密钥调用访问交换接口，两个请求均返回 200、角色正确且成功设置会话 Cookie；没有在验证输出中显示密钥值。

## 2026-07-13：后端 B11–B14 实现

- 任务摘要：执行历史统计、宠物查询、喂食事务、玩耍冷却和宠物改名，完成第一版业务闭环。
- AI 贡献：实现按工作空间隔离的月份/日期历史查询和不可变计划快照；实现权威宠物状态；实现 FIFO 食物消费、负向流水、经验/亲密度增长、连续升级、成长阶段变化和宠物事件的单事务喂食；实现服务端玩耍冷却及最后写入生效的改名事件。
- 并发设计：所有喂食先锁定工作空间宠物行，再锁定可消费奖励流水；同一宠物的并发动作串行结算，最后一份食物只允许一个会话消费。Vitest 数据库文件改为串行执行，避免多个测试文件同时清空同一测试库，但单项业务并发测试仍真实并行发起请求。
- 测试：Supabase PostgreSQL 上后端测试 28/28 通过；新增覆盖历史快照在改名/归档后保持不变、月份自然日、宠物状态、连续升级与 evolve 事件、最后一份食物并发竞争、食物不足无部分修改、玩耍冷却无部分修改，以及两种角色连续改名事件。
- 调试记录：测试完成后恢复 demo 种子时，首次内部轮换脚本错误使用隔离环境不存在的 `process` 对象，导致一组新测试密钥生成但未保存且未泄露；随后改为内部读取现有 `.env` 完成密钥轮换。最终使用 participant 会话对 pet、history、rename 和 play 路由做真实 API smoke test，全部返回 200。
- 最终验证：contracts/api 的 lint、typecheck、build 和格式检查全部通过；共享契约测试 2/2 通过；Drizzle 报告无待生成迁移；只读执行前端 typecheck 确认共享契约兼容并通过。数据库业务全量测试仍以本轮较早执行的 28/28 结果为准，最终检查阶段没有再次清空数据库，以保留恢复后的 demo 种子和本地访问密钥。
- 隐私检查：本记录和验证输出没有复制数据库密码、Cookie Secret、pepper 或最终保存的访问密钥。

## 2026-07-13：Windows 迁移路径兼容修复

- 问题：Windows 环境执行 `db:migrate` 时，Drizzle 报告找不到 `meta/_journal.json`。
- 原因：代码使用 `URL.pathname` 作为文件系统路径，在 Windows 生成带前导斜杠的 `/C:/...` 路径。
- 修复：集中使用 Node.js `fileURLToPath()` 生成跨平台迁移目录，并同步替换 migrate、reset-test-db 和两份 PostgreSQL 集成测试中的路径构造。
- 验证：API lint、typecheck、build 通过，无数据库测试 17/17 通过。Linux 执行迁移已越过迁移文件读取阶段，不再出现 journal 路径错误，但随后 Supabase 返回 `28P01` 密码认证失败；只读重连同样确认当前本地数据库凭据已失效。因此没有把本次实库迁移记录为通过，Windows 仍需用户拉取修复后复验。

## 2026-07-13：前后端首次 GitHub 交付准备

- 任务摘要：按用户要求将两个终端产出的前端、后端、共享契约和项目文档提交到 `jimSemfoundry/check-in`。
- AI 贡献：检查全仓文件与忽略规则；运行 lint、类型检查、单元/集成测试、生产构建、格式检查和浏览器核心流程；修复前端单元测试被本机 `.env.local` 意外切换到真实 API 而长期停留加载态的问题；统一格式化前端文件。
- 过程纠正：首次全仓测试中前端 5 项测试有 3 项失败。根因是 Vitest 未固定 `VITE_USE_MOCK_API`，导致测试结果依赖未提交的本地环境变量；在 Vitest 配置中显式启用 Mock API 后，前端测试恢复为 5/5。首次格式检查发现 37 个前端文件未格式化，执行项目 Prettier 后通过。
- 验证：共享契约测试 2/2、前端测试 5/5、后端测试 28/28、Mock 浏览器核心流程 5/5 通过；全仓 lint、typecheck、build 和 format check 通过。
- 验证范围：本轮浏览器流程使用 Mock API；真实 PostgreSQL 后端业务验证沿用本日志同日已记录的 Supabase 集成测试结果。本轮未再次执行会修改测试数据库的真实浏览器联调流程。
- 隐私检查：提交前将再次扫描暂存内容；`.env`、`.env.local`、依赖目录和构建产物已由 `.gitignore` 排除。
- 交付结果：前端、后端、共享契约、迁移、测试、宠物资源和文档已在提交 `aa9a5ce` 推送到远程 `main`。提交包含 118 个文件和 16,011 行新增；暂存扫描未发现未披露的凭据形态内容。
- 远程限制：空仓库写权限测试产生的 `codex-access-test` 仍是 GitHub 默认分支，因此无法通过 Git 删除；需由仓库管理员先把默认分支切换为 `main`，随后再删除测试分支。

## 2026-07-13：Cloudflare 前端干净构建修复复核

- 任务摘要：复核前端终端为 Cloudflare 干净环境修复的打包问题，判断是否需要修改 Pages 构建输入参数，并推送代码。
- 人工修改：`apps/web/package.json` 的 `build` 脚本增加 `pnpm --filter @soft-habit/contracts build`，确保前端 TypeScript/Vite 构建前生成共享契约产物。
- AI 贡献：审阅修改及 monorepo 依赖关系；移走本地 `packages/contracts/dist` 后模拟干净环境；确认原 Pages 外部命令无需变更，仍从仓库根目录执行 `pnpm --filter web build`，输出目录仍为 `apps/web/dist`。
- 验证：干净模拟构建成功并转换 204 个模块；前端 lint、typecheck、测试 5/5 和目标文件格式检查通过。
- 隐私检查：修改仅涉及公开的 package 脚本和本记录，没有环境变量值或凭据。

## 2026-07-18：Tiny Swords 浮岛地图第一阶段方案

- 任务摘要：根据用户提供的 Tiny Swords tilemap guide、素材包结构和参考截图，为另一个终端执行的第一阶段静态地图原型编写方案。
- AI 贡献：验证素材包包含地形、浪花、海面、云、树、石头、羊、建筑、单位和 UI 资源；分析参考截图的浮岛经营画面结构；将首个里程碑收敛为“正方形浮岛、海、浪、云、少量资源物”的静态视觉验证。
- 人工决策：用户希望第一步先看到正方形浮岛地图，周围有浪、海和云；暂不进入建造、资源、战斗或 APK 打包。
- 产出：`docs/superpowers/specs/2026-07-18-floating-island-map-design.md`。
- 验证：本轮只写方案文档，没有实现或运行前端代码；文档列出后续执行方需要运行的 `pnpm --filter web typecheck` 和 `pnpm --filter web build`。
- 隐私检查：未记录下载链接签名、访问密钥、Token、数据库连接串或其他秘密信息。
